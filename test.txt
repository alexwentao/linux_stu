    ~ 表示用户的家目录 
    cd 不加参数 可以直接进入用户 “家目录” cd ~ 等于cd
    cd ~tom/  进入tom的家目录
    

     删除同理（但是需要删除b后a为空才能成功删除a 否则只删除b）

    cp  /root/a /root/xx/yy    若yy为文件夹，则把a拷贝到yy文件夹中  若yy为文件或者不存在   则把a‘尝试’覆盖（不通类型无法覆盖）yy或者拷贝重命名为yy   不会复制文件属性信息（修改时间）  加-p  就可以复制属性信息
    -a可以复制目录和属性 
